{% extends 'base.html.twig' %}

{% block title %}Cart{% endblock %}

{% block body %}
    <h1>Cart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CartId</th>
                <td>{{ cart.cartId }}</td>
            </tr>
            <tr>
                <th>ProductQte</th>
                <td>{{ cart.productQte }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ cart.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cart_index') }}">back to list</a>

    <a href="{{ path('cart_edit', {'cartId': cart.cartId}) }}">edit</a>

    {{ include('cart/_delete_form.html.twig') }}
{% endblock %}
